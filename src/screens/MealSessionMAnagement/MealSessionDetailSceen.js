import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

const MealSessionDetailSceen = () => {
  return (
    <View>
      <Text>MealSessionDetailSceen</Text>
    </View>
  )
}

export default MealSessionDetailSceen

const styles = StyleSheet.create({})